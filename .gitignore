*.iml
.gradle/
.kotlin/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks
keystore.properties

# debug.keystore is intentionally committed (see app/build.gradle.kts) — it's Android's
# well-known, non-secret debug signing key, shared so local and CI-built debug APKs
# always sign identically and can be installed over one another.
!/debug.keystore
